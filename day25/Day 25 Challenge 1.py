import numpy

targetX = 10
targetY = 10

gridDictionary = {}

xAxisValue = 1
yAxisValue = 1
valueToPlace = 1

grid = numpy.zeros((targetY, targetX))

grid[yAxisValue - 1][xAxisValue - 1] = valueToPlace

# now change y value
target = 2

stepCounter = 1

# while grid[targetX - 1][targetY - 1] == 0:
while stepCounter < 10
        for step in range(1, stepCounter, 1):


print grid
